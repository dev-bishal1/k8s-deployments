apiVersion: apps/v1
kind: Deployment
template:
  name: restaurant-api
  labels:
    tier: backend
    app: restaurant-api
spec:
  replicas: 3
  selector:
    matchLabels:
      app: restaurant-api
  template:
    metadata:
      labels:
        app: restaurant-api
    spec:
      containers:
        - name: restaurant-api-container
          image: bishal12/restaurant-api:staging
          resources:
            requests:
              cpu: "250m"
              memory: "100Mi"
            limits:
              cpu: "500m"
              memory: "500Mi"
  strategy:
    rollingUpdate:
      maxSurge: 5
      maxUnavailable: 1


        